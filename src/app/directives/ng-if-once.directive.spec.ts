import { NgIfOnceDirective } from './ng-if-once.directive';

describe('NgIfOnceDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfOnceDirective();
    expect(directive).toBeTruthy();
  });
});
